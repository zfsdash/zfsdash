package web

// v0.3.2 routes registered in RegisterRoutes via init pattern
// Capacity plan: GET /api/pools/{pool}/capacity-plan
// Snapshot forensics: GET /api/pools/{pool}/snapshot-forensics
// These are added to the main routes.go file
